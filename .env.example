# Example environment file — copy to `.env` and fill in real values.
# On the server, init_vps.sh generates .env automatically; this file exists
# only to document the variables and to keep .env out of git.

REA_SECRET_KEY=change-me-to-a-long-random-string
REA_DEBUG=false
REA_DOMAIN=localhost